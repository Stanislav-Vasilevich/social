import React from "react";
import styles from "./Else.module.css";

const {main} = styles;

function Else() {
  return (
    <section className={main}>
      <h2>Eще</h2>
    </section>
  )
}

export default Else;