import React from 'react';
import styles from './../../../../../App.module.css';
import s from './Forum.module.css';

const Forum = () => {
	return (
		<div className={`${styles.page} ${s.forum}`}>
			<h1>forum</h1>
		</div>
	);
};

export default Forum;
